# Versions explicitly recorded in the executed analysis
numpy==2.1.3
pandas==2.3.3
scipy==1.16.3
anndata==0.13.3.post0
pydeseq2==0.5.4
scanpy==1.12.4
harmonypy==2.0.0
leidenalg==0.12.0
igraph==1.0.0
scikit-misc==0.5.2

# Used by the pipeline, but their exact historical versions were not recorded.
# They are intentionally left unpinned in this archival release rather than
# assigning versions that were not verified from the executed environment.
gseapy
matplotlib
requests
openpyxl
h5py
pypdf
